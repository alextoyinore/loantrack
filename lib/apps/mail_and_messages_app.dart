import 'package:flutter/material.dart';

class EmailAndMessages extends StatefulWidget {
  const EmailAndMessages({Key? key}) : super(key: key);

  @override
  State<EmailAndMessages> createState() => _EmailAndMessagesState();
}

class _EmailAndMessagesState extends State<EmailAndMessages> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
