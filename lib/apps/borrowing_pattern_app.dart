import 'package:flutter/material.dart';

class BorrowingPattern extends StatefulWidget {
  const BorrowingPattern({Key? key}) : super(key: key);

  @override
  State<BorrowingPattern> createState() => _BorrowingPatternState();
}

class _BorrowingPatternState extends State<BorrowingPattern> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
